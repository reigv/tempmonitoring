#include <stdio.h>

int main() {
    printf("Temp monitor");

    int valid_entry = 0; //flag
    int t_high = 0;
    int t_low =0;
    do {
        printf("Enter the high of the day");
        scanf("%d", &t_high);


    } while (valid_entry = 0);
}